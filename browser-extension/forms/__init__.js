/**
 * FormMaster Forms Package.
 * This package contains form modules for different universities.
 */

const formBase = require('./form_base');
const formUtils = require('./form_utils');
const formExtract = require('./form_extract');

module.exports = {
  formBase,
  formUtils,
  formExtract
};
